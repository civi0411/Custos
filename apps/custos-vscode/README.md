# Custos VS Code Extension

This directory contains the upcoming TypeScript VS Code extension for Custos.
It will connect to the local `custosd` IPC socket to display task status, continuation packets,
and diff review interfaces directly inside VS Code.
